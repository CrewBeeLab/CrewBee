project_id: crewbee
run_status: running
active_cycle: C1
active_step_id: S4
last_checkpoint: CP-0007
blockers: []
next_actions:
  - action: Before publishing 0.1.12, bump release metadata/tag from the current 0.1.11 package state and verify final release notes against the release commit.
    owner: active-agent
    source: latest update payload
  - action: Use the internal 0.1.12 release-notes basis only as source material; avoid overstating runtime API-error fallback or real provider catalog auto-discovery.
    owner: active-agent
    source: latest update payload
  - action: For model/provider follow-up, remember current fallback is projection-time selection only; it does not retry after real LLM runtime API failures.
    owner: active-agent
    source: latest update payload
  - action: Preserve current model-resolution fallback ordering and host-default projection semantics when extending the helperized resolver.
    owner: active-agent
    source: latest update payload
  - action: If catalog-aware model availability is required, wire `availableModels` from a real OpenCode or Models.dev source before claiming provider availability detection beyond injected test/catalog data.
    owner: active-agent
    source: latest update payload
  - action: For any follow-up on the productized install flow, preserve CLI compatibility for existing install/uninstall commands and compatibility exports while keeping setup/update as the recommended user path.
    owner: active-agent
    source: latest update payload
  - action: Preserve compatibility re-export paths when using clearer OpenCode config-file and install/package responsibility modules.
    owner: active-agent
    source: DECISIONS.md
  - action: Keep OpenCode detection part of setup/doctor health semantics; a completed OpenCode install must still leave opencode available in PATH before setup is considered successful.
    owner: active-agent
    source: latest update payload
