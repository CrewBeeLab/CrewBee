{
  "name": "agentscroll",
  "version": "0.1.0",
  "private": true,
  "description": "Portable Agent Pack system scaffold",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
