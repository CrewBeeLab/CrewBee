id: general-team.shared-capabilities
kind: shared-capabilities
version: 1.0.0

models:
  default: balanced-default
  available:
    - balanced-default
    - reasoning-high
    - exploration-high

tools:
  default_profile: repo-readwrite
  available_profiles:
    - repo-readonly
    - repo-readwrite
    - web-research

skills:
  shared: []

instruction_packs:
  shared:
    - repo-core
    - agentscroll-team-framework

memory:
  default_profile: project-memory

hooks:
  default_bundle: repo-guardrails

mcp:
  shared_servers:
    - github
